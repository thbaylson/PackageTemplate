# Baylson Games Analytics Helper

Designer-friendly helpers for recording Unity Analytics custom events from ScriptableObject definitions.

Use `AnalyticsEventDefinition` assets to define event names and parameters once, then record those events from scene emitters or gameplay code. See the package documentation for setup, Unity Cloud Dashboard workflow, and activation responsibilities.
