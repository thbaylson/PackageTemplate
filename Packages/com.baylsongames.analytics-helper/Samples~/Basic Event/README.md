# Basic Event Sample

This sample shows a small script that records a configured `AnalyticsEventDefinition` with a runtime `level_id` override.

Before using the sample, create a matching custom event in Unity Cloud Dashboard and a local event definition asset with a `level_id` string parameter.
